<h1> List of all Pages </h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
  </tr>

  <% @pages.each do |page| %>
    <tr>
      <td><%= page.title %></td>
      <td><%= link_to 'Show', page_path(page), data: { slug: page.slug } %></td>
    </tr>
  <% end %>
</table>
