<aside class="sidebar">
  <nav class="sidebar-nav">
    <h1> List of all Pages </h1>
      <ul>
        <% @pages.each do |page| %>
          <li>
            <a href="<%= page_path(page) %>"><%= page.title %></a>
          </li>
        <% end %>
      </ul>
  </nav>
</aside>
